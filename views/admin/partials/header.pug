header.header 
    .container-fluid
        .row.align-items-center 
            .col-3
                .inner-logo 
                    a(href="/admin/dashboard") Admin
            .col-9
                .text-end     
                    a.btn.btn-primary.me-2(
                    href=`${prefixAdmin}/my-account`
                    style="padding:2px 8px;font-size:1rem;"
                    )= user.fullName

                    a.btn.btn-danger(
                    href=`${prefixAdmin}/auth/logout`
                    style="padding:2px 8px;font-size:1rem;"
                    ) Đăng xuất                    